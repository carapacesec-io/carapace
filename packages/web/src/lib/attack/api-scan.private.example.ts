/**
 * OPEN-SOURCE STUB — API scan returns no findings.
 *
 * Copy this file to `api-scan.private.ts` to build the project.
 */

import type { ScanFinding } from "./auth-audit";

export async function runApiScan(_targetUrl: string): Promise<ScanFinding[]> {
  return [];
}
