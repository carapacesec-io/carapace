/**
 * OPEN-SOURCE STUB — Recon scan returns no findings.
 *
 * Copy this file to `recon.private.ts` to build the project.
 */

import type { ScanFinding } from "./auth-audit";

export async function runRecon(_targetUrl: string): Promise<ScanFinding[]> {
  return [];
}
